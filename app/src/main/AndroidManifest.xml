<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sunshinexu.mobilelearn">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".app.MobileLearnApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_logo"
        android:label="@string/app_name"

        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.main.ui.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".activity.main.ui.LoginActivity"/>
        <activity android:name=".activity.main.ui.ArticleDetailActivity" />
        <activity android:name=".activity.main.ui.KnowledgeDetailActivity"/>
        <activity android:name=".activity.main.ui.ItemActivity"/>
        <activity android:name=".activity.main.ui.SearchActivity"/>

    </application>

</manifest>